## Nix Deployment

### Deploying Backend

Need to set up instructions for just cross compiling locally and copying to the remote. Maybe in root home somewhere. Too cumbersome to package with nix (builds take too long to iterate).

### Deploying Frontent

The frontend is simply stored in `/var/www/html/faux_show`