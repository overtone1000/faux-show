[package]
name = "livekit-wakeword-test"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
cpal = "^0" #Get microphone input
livekit-wakeword = "^0"
circular-buffer = "^1"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1